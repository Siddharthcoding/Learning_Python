data = ["Apple", "Mango", 4, 21.5, True, "Bird"]  # List with different data types

print(data[0])

data[0] = "Grapes"  # Unlike strings, lists are mutable i.e they can be changed
print(data[0])

print(data[1:4])  # Slicing the list